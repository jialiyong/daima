1、安卓播放器视频比例确认。（19：6）（5：4）。

2、bottom从新做。

3、H5同层播放针对video原生视频标签做。

4、腾讯X5 同层播放器方案。奥点云自己播放器插件，对与同层暂时没有方案实现。

5、同层播放器是H5同层交互。所以直播间都是原生video 实现，用他们的推流链接。
（流支持待测，目前用阿里直播间的流支持。）。



1、安卓H5同层交互，腾讯给出的方案，就是针对安卓手机视频全屏问题。
2、在阿里云直播间嵌入。达到基本效果。存在问题
问题：
	1、进入全屏上下有黑边，腾讯给出的方案解决不了，
	2、视频出现位置不固定，应该出现在最顶端，但是有时候出现在中间（机率在30%左右）。
出现问题原因：
	1、同层播放是H5 video原生标签做同层交互。（用插件做的不了解内部逻辑。导致同层视频播放时有时候在中间有时候在上边，不用插件，原生写的没出现这种情况）
	2、底部聊天问答等高度等问题（底部高度不规范问题导致调）
//解决第三方软键盘唤起时底部input输入框被遮挡问题
    var bfscrolltop = document.body.scrollTop;//获取软键盘唤起前浏览器滚动部分的高度
    $("input.inputframe").focus(function(){//在这里‘input.inputframe’是我的底部输入栏的输入框，当它获取焦点时触发事件
        interval = setInterval(function(){//设置一个计时器，时间设置与软键盘弹出所需时间相近
        document.body.scrollTop = document.body.scrollHeight;//获取焦点后将浏览器内所有内容高度赋给浏览器滚动部分高度
        },100)
    }).blur(function(){//设定输入框失去焦点时的事件
        clearInterval(interval);//清除计时器
        document.body.scrollTop = bfscrolltop;将软键盘唤起前的浏览器滚动部分高度重新赋给改变后的高度
    });